{
  "mode": "agent",
  "control_plane_url": "{{ cp_url | default('http://localhost:8080') }}",
  "skip_attestation": {{ dd_skip_attestation | default(true) | lower }},
  "node_size": "{{ agent_node_size | default('standard') }}",
  "datacenter": "{{ dd_env }}"
}
