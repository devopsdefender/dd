{
  "app_name": "mount-data",
  "cmd": [
    "/bin/busybox", "sh", "-c",
    "set -e\nDATA_DIR=/var/lib/easyenclave/data\nif [ -b /dev/vdc ]; then\n  mkdir -p \"$DATA_DIR\"\n  mount -t ext4 /dev/vdc \"$DATA_DIR\"\n  echo \"mount-data: mounted /dev/vdc on $DATA_DIR\"\nelse\n  echo mount-data: no /dev/vdc, skipping\nfi"
  ]
}
