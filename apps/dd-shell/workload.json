{
  "app_name": "dd-shell",
  "expose": { "hostname_label": "shell", "port": 7682 },
  "env": [
    "DD_MODE=shell",
    "DD_SHELL_PORT=7682",
    "DD_SHELL_DIR=/tmp/dd-shell",
    "DD_SHELL_EE_SOCKET=/var/lib/easyenclave/agent.sock"
  ],
  "cmd": [
    "/bin/busybox", "sh", "-c",
    "set -e; until [ -x /var/lib/easyenclave/bin/devopsdefender ]; do sleep 1; done; sleep 5; cp /var/lib/easyenclave/bin/devopsdefender /tmp/dd-shell-bin; chmod +x /tmp/dd-shell-bin; exec /tmp/dd-shell-bin shell"
  ]
}
