#!/bin/bash
# Finalize: cleanup for reproducibility
set -euo pipefail

if [ -z "${BUILDROOT:-}" ]; then
    echo "ERROR: BUILDROOT not set"
    exit 1
fi

rm -rf "$BUILDROOT/root/.cache" 2>/dev/null || true
rm -rf "$BUILDROOT/tmp"/* 2>/dev/null || true

echo "==> Finalize complete"
